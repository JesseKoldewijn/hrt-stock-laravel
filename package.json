{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "dev:laravel": "php artisan serve",
        "full:dev": "dotenv concurrently \"npm run dev\" \"npm run dev:laravel\"",
        "build": "vite build"
    },
    "devDependencies": {
        "@prettier/plugin-php": "^0.22.1",
        "axios": "^1.6.1",
        "concurrently": "^8.2.2",
        "dotenv-cli": "^7.3.0",
        "laravel-vite-plugin": "^1.0.0",
        "prettier": "^3.1.1",
        "vite": "^5.0.0"
    }
}
